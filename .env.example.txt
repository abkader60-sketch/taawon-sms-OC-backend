# =============================================================================
# Ta'awon P3 SMS - Environment Variables
# =============================================================================
# Copy this file to .env (for local dev) and fill in real values.
# DO NOT commit .env to Git - it contains secrets.
#
# On Railway, set each of these in the project's "Variables" panel instead
# of uploading a .env file. Railway injects them at runtime.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# LOCAL: point at the portable Postgres on localhost:5432
# RAILWAY: when you provision Postgres on Railway, it auto-sets DATABASE_URL.
#          The app reads DATABASE_URL first; if not present, falls back to
#          the individual DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD vars.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=id_system
DB_USER=postgres
DB_PASSWORD=__SET_THIS__

# Optional: if set, the app uses this single DSN and ignores the five vars above.
# Railway will set this for you automatically when you attach a Postgres service.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# -----------------------------------------------------------------------------
# SMTP (for sending emails - rejection notices, external submitter credentials)
# -----------------------------------------------------------------------------
# Use a Gmail App Password (NOT your regular Gmail password).
# https://myaccount.google.com/apppasswords (2-step verification must be ON)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=youraccount@gmail.com
SMTP_PASSWORD=__16_CHAR_APP_PASSWORD_NO_SPACES__
SMTP_FROM=youraccount@gmail.com

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing) - which web origins can call the API
# -----------------------------------------------------------------------------
# LOCAL DEV: leave as * (the file:// frontend has no real origin).
# PRODUCTION: set to your Railway frontend URL, e.g.
#   CORS_ORIGINS=https://taawon-sms.up.railway.app
# Multiple origins: comma-separate them.
CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# Server port (Railway sets this automatically; locally we hardcode 8000)
# -----------------------------------------------------------------------------
# PORT=8000
